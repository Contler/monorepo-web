import { IsActiveModulePipe } from './is-active-module.pipe';

describe('IsActiveModulePipe', () => {
  it('create an instance', () => {
    const pipe = new IsActiveModulePipe();
    expect(pipe).toBeTruthy();
  });
});
